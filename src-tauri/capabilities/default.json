{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Stint main window: core defaults, native window resize for the compact toggle, completion notifications",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "core:window:allow-start-dragging",
    "notification:default"
  ]
}
